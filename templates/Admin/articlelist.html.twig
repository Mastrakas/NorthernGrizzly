{% extends "baseadmin.html.twig" %}
{% block title %}Accueil Administrateur{% endblock %}

{% block main %}
    <h1 class="box text-center py-2 text-capitalize font-weight-bold mx-0">Liste des articles</h1>
    {% for article in articlesActu %}
        {% if loop.index is divisible by(2) %}

            <div class="row presentation mx-0">
                <b class="col-4 offset-2">{{ article.title }}</b>
                <p class="col-4">{{ article.summary }}</p>
                <div class="col-2">
                    <a href="{{ path('admin_article_update', {'id':article.id}) }}" class="bouton">Modifier l'article ici</a><br>
                    <a href="{{ path('admin_article_delete', {'id':article.id}) }}" class="bouton">Supprimer l'article ici</a>
                </div>
            </div>
            <hr>

        {% else %}

            <div class="row presentation mx-0">
                <p class="col-4 offset-2">{{ article.summary }}</p>
                <b class="col-4">{{ article.title }}</b>
                <div class="col-2">
                    <a href="{{ path('admin_article_update', {'id':article.id}) }}" class="bouton">Modifier l'article ici</a><br>
                    <a href="{{ path('admin_article_delete', {'id':article.id}) }}" class="bouton">Supprimer l'article ici</a>
                </div>
            </div>
            <hr>

        {% endif %}

    {% endfor %}

    <hr>

    {% for training in articlesTraining %}
        {% if loop.index is divisible by(2) %}

            <div class="row presentation mx-0">
                <b class="col-4 offset-2">{{ training.title }}</b>
                <p class="col-4">{{ training.summary }}</p>
                <div class="col-2">
                    <a href="{{ path('admin_article_update', {'id':training.id}) }}" class="bouton">Modifier l'article ici</a><br>
                    <a href="{{ path('admin_article_delete', {'id':training.id}) }}" class="bouton">Supprimer l'article ici</a>
                </div>
            </div>
            <hr>

        {% else %}

            <div class="row presentation mx-0">
                <p class="col-4 offset-2">{{ training.summary }}</p>
                <b class="col-4">{{ training.title }}</b>
                <div class="col-2">
                    <a href="{{ path('admin_article_update', {'id':training.id}) }}" class="bouton">Modifier l'article ici</a><br>
                    <a href="{{ path('admin_article_delete', {'id':training.id}) }}" class="bouton">Supprimer l'article ici</a>
                </div>
            </div>
            <hr>

        {% endif %}

    {% endfor %}

{% endblock %}